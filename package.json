{
  "dependencies": {
    "storeroom": "git://github.com/jcoglan/storeroom.git#master",
    "@escodb/core": "github:escodb/core",
    "@escodb/couchdb-adapter": "github:escodb/couchdb-adapter"
  },
  "devDependencies": {
    "mocha": "^10.7.3"
  },
  "scripts": {
    "test": "mocha --recursive"
  }
}
